import "./Loading.css";

const Loading = () => {
  return (
    <div>
      <button id="loading" className="loading">
        <div className="spinner" />
      </button>
    </div>
  );
};

export default Loading;
